<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav"
    app:startDestination="@id/tasksFragment">

    <fragment
        android:id="@+id/tasksFragment"
        android:name="views.TasksFragment"
        android:label="tasks_fragment"
        tools:layout="@layout/tasks_fragment" >
        <action
            android:id="@+id/goToTask1"
            app:destination="@id/connectsFragment" />
    </fragment>
    <fragment
        android:id="@+id/connectsFragment"
        android:name="views.ConnectsFragment"
        android:label="connects_fragment"
        tools:layout="@layout/connects_fragment" />
</navigation>